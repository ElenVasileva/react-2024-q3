import styles from './Header.module.css';

const Header = () => (
  <>
    <div className={styles.header}>
      <div className={styles.appName}>React Forms Application</div>
    </div>
  </>
);

export default Header;
